# Working agreements

- **Commits are pre-authorized.** Don't ask "should I commit?" before committing —
  just commit when the work is done (still branch off `master` first if you're on
  it). This overrides the default "only commit when asked" behavior for this repo
  specifically. Pushing to the remote and opening PRs still need an explicit ask.
